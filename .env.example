# Copy this file to .env and fill in your values
# cp .env.example .env

OPENAI_API_KEY=
GOOGLE_API_KEY=

# MCP Server URL (set by admin — points to the deployed MCP server)
# Google Ads and Drive credentials are held server-side, not here.
DISTL_MCP_SERVER_URL=
